name: Update Flex endpoint

on:
    push:
        branches:
            - main

defaults:
    run:
        shell: bash

jobs:
    call-flex-update:
        uses: robmeijer/symfony-flex/.github/workflows/callable-flex-update.yml@main
        with:
            branch: main
            versions_json: .github/versions.json
